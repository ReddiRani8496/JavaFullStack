//Example: Type Annotation of parameters

function display(id:number, name:string) {
    console.log("Id = " + id +", Name = " + name);
}

//example : type anotation in object

var employee: {
    id:number;
    name:string;
}

employee = {
    id:100,
    name:'rahul'
}

const maxValue:number = 100;
